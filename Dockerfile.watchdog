FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt /app/
RUN pip install --no-cache-dir -r requirements.txt

COPY . /app

ENV PYTHONUNBUFFERED=1
ENV PYTHONPATH=/app

CMD ["python", "-c", "from core.monitoring.watchdog import watchdog; import asyncio; asyncio.run(watchdog.monitor_loop())"]
